# IT-Inventory-Management-System
The project is a command-line interface (CLI) application designed to manage an IT equipment inventory. It follows a modular architecture by separating the database connection logic, the data manipulation functions, and the user interface into three distinct files
